@extends('backend.layouts.master')
@section('title','Targets')
@section('content')<div class="main-content"><div class="page-content"><div class="container-fluid"><div class="card"><div class="card-header d-flex align-items-center"><h5 class="mb-0 flex-grow-1">Target Master</h5>@can('create', \App\Models\Target::class)<a href="{{ route('admin.targets.create') }}" class="btn btn-sm btn-primary">Add Target</a>@endcan</div><div class="card-body"><div class="table-responsive"><table class="table table-bordered"><thead><tr><th>#</th><th>Target Name</th><th>Monthly Target</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($targets as $target)<tr><td>{{ $loop->iteration }}</td><td>{{ $target->target_name }}</td><td>{{ number_format($target->monthly_target) }}</td><td><span class="badge {{ $target->status ? 'bg-success-subtle text-success' : 'bg-danger-subtle text-danger' }}">{{ $target->status ? 'Active' : 'Inactive' }}</span></td><td>@can('edit', \App\Models\Target::class)<a href="{{ route('admin.targets.edit', $target) }}" class="text-info fs-4 me-2"><i class="bx bxs-edit"></i></a>@endcan @can('delete', \App\Models\Target::class)<form class="d-inline" method="post" action="{{ route('admin.targets.destroy', $target) }}" onsubmit="return confirm('Delete this target?')">@csrf @method('DELETE')<button class="btn btn-link text-danger fs-4 p-0"><i class="bx bxs-trash"></i></button></form>@endcan</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">No targets configured.</td></tr>@endforelse</tbody></table></div></div></div></div></div></div>@endsection
