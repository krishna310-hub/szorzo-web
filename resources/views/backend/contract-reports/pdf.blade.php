<!doctype html><html><head><meta charset="utf-8"><title>Contract Report {{ $month->format('F Y') }}</title>
<style>body{font-family:DejaVu Sans,sans-serif;color:#222;font-size:10px}h1{margin:0 0 4px;font-size:20px}.meta{color:#666;margin-bottom:18px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:7px}th{background:#eee;text-align:left}.num{text-align:right}tfoot{font-weight:bold;background:#f3f3f3}</style></head><body>
<h1>Contract Report</h1><div class="meta">{{ $month->format('F Y') }} &middot; {{ $month->daysInMonth }} calendar days &middot; Generated {{ now()->format('d M Y, h:i A') }}</div>
<table><thead><tr><th>#</th><th>Candidate</th><th>Client</th><th>Job Role</th><th>Recruiter</th><th class="num">Monthly Take Home</th><th class="num">Present</th><th class="num">Leave Days</th><th class="num">Worked Hours</th><th class="num">Payable Salary</th></tr></thead><tbody>
@forelse($reports as $report)<tr><td>{{ $loop->iteration }}</td><td>{{ $report->candidate?->candidate_name }}</td><td>{{ $report->candidate?->client?->client ?? '—' }}</td><td>{{ $report->candidate?->jobRole?->job_role ?? '—' }}</td><td>{{ $report->candidate?->recruiter?->recruiter_name ?? '—' }}</td><td class="num">INR {{ number_format($report->monthly_take_home, 2) }}</td><td class="num">{{ $report->present_days }}</td><td class="num">{{ $report->absent_days }}</td><td class="num">{{ $report->worked_hours !== null ? number_format($report->worked_hours, 2) : '—' }}</td><td class="num">INR {{ number_format($report->payable_salary, 2) }}</td></tr>@empty<tr><td colspan="10" style="text-align:center">No contract records found.</td></tr>@endforelse
</tbody>@if($reports->count())<tfoot><tr><td colspan="5">Total ({{ $reports->count() }} candidates)</td><td class="num">INR {{ number_format($reports->sum('monthly_take_home'), 2) }}</td><td class="num">{{ $reports->sum('present_days') }}</td><td class="num">{{ $reports->sum('absent_days') }}</td><td class="num">{{ number_format($reports->sum('worked_hours'), 2) }}</td><td class="num">INR {{ number_format($reports->sum('payable_salary'), 2) }}</td></tr></tfoot>@endif</table></body></html>
